<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.youtu.dao.LosterDao">
    <select id="queryByAgeAndGender" resultType="String">
        SELECT picture
        FROM loster
        WHERE age = #{age} AND gender = #{gender};
    </select>

    <select id="queryByMaxMinAgeAndGender" resultType="String">
        SELECT picture
        FROM loster
        WHERE age >= #{minAge} AND age <![CDATA[ <= ]]> #{maxAge} AND gender = #{gender};
    </select>
</mapper>