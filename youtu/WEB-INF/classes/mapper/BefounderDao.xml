<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.youtu.dao.BefounderDao">
    <insert id="insertBefounder">
        INSERT INTO befounder
        (uuid, founder_uuid, found_location, found_time, picture, remarks, state) VALUES
        (#{uuid}, #{founderUuid}, #{foundLocation}, #{foundTime}, #{picture}, #{remarks}, #{state})
    </insert>

    <select id="queryByFounderUuid" resultType="com.youtu.entity.Befounder">
        SELECT
        uuid,
        found_location,
        found_time,
        picture,
        age,
        age_range,
        gender,
        remarks
        FROM befounder
        WHERE founder_uuid = #{founderUuid};
    </select>

    <select id="queryByUuid" resultType="com.youtu.entity.Befounder">
        SELECT *
        FROM befounder
        WHERE uuid = #{uuid};
    </select>

    <update id="uploadAgeAndGender">
        UPDATE befounder
        SET age = #{age}, age_range = #{ageRange}, gender = #{gender}, state = #{state}
        WHERE uuid = #{uuid};
    </update>

    <update id="uploadBefounder">
        UPDATE befounder
        SET age = #{age}, age_range = #{ageRange}, gender = #{gender}, remarks = #{remarks}, state = #{state}
        WHERE uuid = #{uuid};
    </update>

    <select id="queryPictureByMaxMinAgeAndGender" resultType="com.youtu.entity.Befounder">
        SELECT
        picture,
        uuid
        FROM befounder
        WHERE age >= #{minAge} AND age <![CDATA[ <= ]]> #{maxAge} AND gender = #{gender};
    </select>

    <delete id="deleteBefounder">
        DELETE FROM befounder
        WHERE uuid = #{uuid} AND founder_uuid = #{founderUuid};
    </delete>
</mapper>